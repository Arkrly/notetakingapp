# ===== TEST PROFILE =====
spring.application.name=notetakingapp

# Disable Swagger in test
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false

# Use H2 in-memory database for tests
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Hibernate 6.x auto-detects H2 dialect from jdbc:h2: URL — no explicit dialect needed

# Disable Flyway in test
spring.flyway.enabled=false

# JWT
app.jwt.secret=cGWQhI6rLabkMlfHacubRsK80aBqdoxJDy+nOU0haSg=
app.jwt.access-token-expiry-ms=900000
app.jwt.refresh-token-expiry-ms=604800000
