---
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/notetakingapp/repository/NoteRepository.java
  - backend/src/main/java/com/notetakingapp/service/NoteService.java
  - backend/src/test/java/com/notetakingapp/repository/NoteRepositoryTest.java
  - frontend/src/app/notes/notes-list/notes-list.component.ts
  - frontend/src/app/notes/notes-list/notes-list.component.spec.ts
autonomous: true
requirements:
  - FEAT-04
---

# Plan 03: Backend Archive Filtering

**Goal:** Archived notes are properly filtered in backend queries (not just frontend).

## Success Criteria
- Backend queries filter out archived notes by default
- Main note list only returns active (non-archived) notes
- Frontend removes client-side filtering
- Separate endpoint for archived notes if needed

## Tasks

<task id="6.1-03-01">
<name>Add archive filtering to backend repository</name>
<files>
<file>backend/src/main/java/com/notetakingapp/repository/NoteRepository.java</file>
</files>
<action>Add a `@Query` annotation to filter archived notes:
```java
@Query("SELECT n FROM Note n WHERE n.isArchived = false")
List<Note> findAllActiveNotes();
```
Update existing query methods to include `WHERE is_archived = false`.</action>
<verification>Run `mvn test -Dtest=NoteRepositoryTest#findAllActiveNotes` to verify filtering</verification>
</task>

<task id="6.1-03-02">
<name>Update service to use filtered queries</name>
<files>
<file>backend/src/main/java/com/notetakingapp/service/NoteService.java</file>
</files>
<action>Update `getAllNotes()` and other main query methods to use the new filtered repository methods. Ensure archived notes are not returned by default.</action>
<verification>Run `mvn test -Dtest=NoteServiceTest` to verify service uses filtered queries</verification>
</task>

<task id="6.1-03-03">
<name>Remove client-side archive filtering</name>
<files>
<file>frontend/src/app/notes/notes-list/notes-list.component.ts</file>
</files>
<action>Remove the client-side filtering logic at line 185 that filters archived notes. The backend now handles this filtering, so frontend code should be removed.</action>
<verification>Run `npm run test:vitest -- notes-list.spec.ts` to verify list displays correctly</verification>
</task>

<task id="6.1-03-04">
<name>Add unit tests for archive filtering</name>
<files>
<file>backend/src/test/java/com/notetakingapp/repository/NoteRepositoryTest.java</file>
<file>frontend/src/app/notes/notes-list/notes-list.component.spec.ts</file>
</files>
<action>Add test cases for:
- Create 3 notes, archive 1 → verify only 2 returned
- Archived note not in main query results
- Separate endpoint returns archived notes if implemented
- Frontend displays all notes from backend (no client filtering)</action>
<verification>Run full test suite to ensure all tests pass</verification>
</task>
</tasks>

## Must Haves
- Backend repository filters archived notes
- Backend service uses filtered queries
- Frontend removes client-side filtering
- Unit tests verify filtering behavior
- Main note list only shows active notes

## Dependencies
None - this is Wave 1, can start immediately

## Notes
Current implementation filters archived notes only in frontend (notes-list.component.ts:185). This is inefficient and incorrect - backend should handle filtering to reduce data transfer and ensure single source of truth.