---
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/notes/note-form/note-form.component.ts
  - frontend/src/app/notes/note-form/note-form.component.spec.ts
autonomous: true
requirements:
  - FEAT-02
  - FEAT-03
---

# Plan 02: Pin/Archive Preservation

**Goal:** Pin status and archive status are preserved when note is updated (not reset to false).

## Success Criteria
- Pin status is preserved when note is updated
- Archive status is preserved when note is updated
- Update request includes isPinned and isArchived fields
- Backend null check logic works correctly

## Tasks

<tasks>
<task id="6.1-02-01">
<name>Include pin/archive in update request</name>
<files>
<file>frontend/src/app/notes/note-form/note-form.component.ts</file>
</files>
<action>Modify the update request to include `isPinned` and `isArchived` fields from the note object. Currently the request only sends title, content, color, and tags - add the missing fields.</action>
<verification>Run `npm run test:vitest -- note-form.spec.ts` to verify update request includes all fields</verification>
</task>

<task id="6.1-02-02">
<name>Add unit tests for pin/archive preservation</name>
<files>
<file>frontend/src/app/notes/note-form/note-form.component.spec.ts</file>
</files>
<action>Add test cases for:
- Create note, pin it, edit title → verify pin status preserved
- Create note, archive it, edit content → verify archive status preserved
- Create note, pin and archive it, edit → verify both preserved
- Update request includes isPinned and isArchived fields</action>
<verification>Run full test suite to ensure all tests pass</verification>
</task>
</tasks>

## Must Haves
- Frontend update request includes isPinned field
- Frontend update request includes isArchived field
- Pin status preserved on edit
- Archive status preserved on edit
- Unit tests verify preservation behavior

## Dependencies
None - this is Wave 1, can start immediately

## Notes
The backend already has null check logic in `applyUpdate()`:
```java
note.setIsPinned(request.getIsPinned() != null ? request.getIsPinned() : note.getIsPinned());
note.setIsArchived(request.getIsArchived() != null ? request.getIsArchived() : note.getIsArchived());
```
This logic is correct - the bug is in the frontend not sending these fields.