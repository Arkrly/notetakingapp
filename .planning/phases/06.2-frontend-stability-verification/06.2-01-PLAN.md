---
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/notes/notes-list/notes-list.component.ts
  - frontend/src/app/user/profile/profile.component.ts
  - frontend/src/app/auth/login/login.component.ts
  - frontend/src/app/auth/register/register.component.ts
autonomous: true
requirements:
  - FRONT-01
  - FRONT-04
---

# Plan 01: Complete RxJS Subscription Cleanup

**Goal:** Add takeUntilDestroyed() to all RxJS subscriptions that don't use async pipe, preventing memory leaks.

## Success Criteria
- All RxJS subscriptions use takeUntilDestroyDestroyed() or async pipe
- No memory leaks from unmanaged subscriptions
- No ExpressionChangedAfterItHasBeenCheckedError in dev mode

## Tasks

<tasks>
<task id="6.2-01-01">
<name>Add takeUntilDestroyed to notes-list component</name>
<files>
<file>frontend/src/app/notes/notes-list/notes-list.component.ts</file>
</files>
<action>Add DestroyRef and takeUntilDestroyed() to all 8 subscriptions in notes-list.component.ts:
- getNotes() subscription
- afterClosed() subscriptions (edit dialog)
- patchNote() subscriptions (pin, archive)
- afterClosed() subscription (delete dialog)</action>
<verification>Run `npm run test:vitest -- notes-list.spec.ts` to verify no regressions</verification>
</task>

<task id="6.2-01-02">
<name>Add takeUntilDestroyed to profile component</name>
<files>
<file>frontend/src/app/user/profile/profile.component.ts</file>
</files>
<action>Verify profile component has takeUntilDestroyed() on valueChanges subscription. Add to any remaining subscriptions.</action>
<verification>Run `npm run test:vitest -- profile.spec.ts` to verify no regressions</verification>
</task>

<task id="6.2-01-03">
<name>Add takeUntilDestroyed to login component</name>
<files>
<file>frontend/src/app/auth/login/login.component.ts</file>
</files>
<action>Add DestroyRef and takeUntilDestroyed() to login() subscription</action>
<verification>Run `npm run test:vitest -- login.spec.ts` to verify no regressions</verification>
</task>

<task id="6.2-01-04">
<name>Add takeUntilDestroyed to register component</name>
<files>
<file>frontend/src/app/auth/register/register.component.ts</file>
</files>
<action>Add DestroyRef and takeUntilDestroyed() to register() subscription</action>
<verification>Run `npm run test:vitest -- register.spec.ts` to verify no regressions</verification>
</task>
</tasks>

## Must Haves
- All subscriptions in notes-list use takeUntilDestroyed() or are removed
- All subscriptions in profile use takeUntilDestroyed() or are removed
- All subscriptions in login use takeUntilDestroyed() or are removed
- All subscriptions in register use takeUntilDestroyed() or are removed

## Dependencies
None - this is Wave 1