---
phase: 06.3-data-layer-refinement
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/notetakingapp/controller/NoteController.java
  - backend/src/main/java/com/notetakingapp/controller/UserController.java
  - backend/src/main/java/com/notetakingapp/service/impl/NoteServiceImpl.java
  - backend/src/main/java/com/notetakingapp/exception/GlobalExceptionHandler.java
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04

must_haves:
  truths:
    - All REST endpoints return DTOs, never JPA entities
    - Note→Tags relationship uses fetch join to prevent N+1 queries
    - Global exception handler returns consistent error format for all errors
    - No LazyInitializationException errors in production logs
  artifacts:
    - path: backend/src/main/java/com/notetakingapp/dto/response/NoteResponse.java
      provides: DTO for note responses
    - path: backend/src/main/java/com/notetakingapp/exception/GlobalExceptionHandler.java
      provides: Consistent error handling
    - path: backend/src/main/java/com/notetakingapp/controller/NoteController.java
      provides: REST endpoints using DTOs
  key_links:
    - from: NoteController.java
      to: NoteResponse.class
      via: return type
      pattern: ResponseEntity<ApiResponse<NoteResponse>>
    - from: NoteServiceImpl.java
      to: NoteMapper.class
      via: toResponse method
      pattern: noteMapper::toResponse
---

<objective>
Verify data layer meets DATA-01 through DATA-04 requirements. Based on code analysis, the implementation already satisfies most requirements - this plan confirms compliance and addresses any gaps.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

# Context from Phase 4 ( Backend Data Layer Fixes):
# - NoteResponse DTO used by all endpoints
# - Tags stored as String, no separate entity
# - GlobalExceptionHandler exists
# - User LAZY fetch filtered by userId
</context>

<tasks>

<task type="auto">
  <name>task 1: Verify all REST endpoints return DTOs (DATA-01)</name>
  <files>backend/src/main/java/com/notetakingapp/controller/NoteController.java,backend/src/main/java/com/notetakingapp/controller/UserController.java</files>
  <action>
    Verify all controller methods return DTOs (NoteResponse, UserResponse, ApiResponse<T>) and never return JPA entities directly. Check:
    - getAllNotes returns Page<NoteResponse>
    - getNoteById returns NoteResponse
    - createNote returns NoteResponse
    - updateNote returns NoteResponse
    - patchNote returns NoteResponse
    - deleteNote returns ApiResponse<Void>
    - All other endpoints use DTOs
    
    No code changes needed if already correct.
  </action>
  <verify>
    <automated>grep -n "ResponseEntity<ApiResponse<.*Response" backend/src/main/java/com/notetakingapp/controller/*.java | wc -l</automated>
  </verify>
  <done>All controller endpoints return DTO response types</done>
</task>

<task type="auto">
  <name>task 2: Verify tags storage (DATA-02)</name>
  <files>backend/src/main/java/com/notetakingapp/entity/Note.java</files>
  <action>
    Verify Note→Tags relationship:
    - Tags are stored as String field in Note entity (line 41: private String tags)
    - No separate Tag entity with @OneToMany relationship exists
    - No fetch join needed because tags is a simple String column
    
    This is already correct - no changes needed.
  </action>
  <verify>
    <automated>grep -n "private String tags" backend/src/main/java/com/notetakingapp/entity/Note.java</automated>
  </verify>
  <done>Tags stored as String, no N+1 query possible</done>
</task>

<task type="auto">
  <name>task 3: Verify global exception handler (DATA-03)</name>
  <files>backend/src/main/java/com/notetakingapp/exception/GlobalExceptionHandler.java</files>
  <action>
    Verify GlobalExceptionHandler returns consistent ApiResponse.error() format for all exceptions:
    - ResourceNotFoundException → 404 + ApiResponse.error
    - UnauthorizedException → 403 + ApiResponse.error
    - ValidationException → 400 + ApiResponse.error
    - MethodArgumentNotValidException → 400 + ApiResponse.error with field errors
    - DataIntegrityViolationException → 409 + ApiResponse.error
    - BadCredentialsException → 401 + ApiResponse.error
    - Generic Exception → 500 + ApiResponse.error
    
    All return ApiResponse with consistent structure.
  </action>
  <verify>
    <automated>grep -c "ApiResponse.error" backend/src/main/java/com/notetakingapp/exception/GlobalExceptionHandler.java</automated>
  </verify>
  <done>All exceptions return consistent ApiResponse.error format</done>
</task>

<task type="auto">
  <name>task 4: Verify no LazyInitializationException risk (DATA-04)</name>
  <files>backend/src/main/java/com/notetakingapp/repository/NoteRepository.java,backend/src/main/java/com/notetakingapp/service/impl/NoteServiceImpl.java</files>
  <action>
    Verify no LazyInitializationException:
    - User relationship uses LAZY fetch (Note.java line 43-45)
    - All repository queries filter by userId, loading User entity inline
    - findNoteAndVerifyOwnership checks ownership after fetch
    - Transactional annotations ensure session is open
    
    Confirm queries use userId filtering that prevents lazy loading issues.
  </action>
  <verify>
    <automated>grep -n "findByUserId\|findByIdAndUserId\|userId" backend/src/main/java/com/notetakingapp/repository/NoteRepository.java | wc -l</automated>
  </verify>
  <done>All queries filter by userId, no LazyInitializationException risk</done>
</task>

</tasks>

<verification>
- Run backend tests: cd backend && ./mvnw test -q
- Verify all DATA-* requirements marked complete in REQUIREMENTS.md
</verification>

<success_criteria>
All 4 DATA requirements satisfied:
- DATA-01: All REST endpoints return DTOs ✓
- DATA-02: Tags as String, no N+1 queries ✓  
- DATA-03: Consistent error format via ApiResponse.error ✓
- DATA-04: No LazyInitializationException risk ✓
</success_criteria>

<output>
After completion, create `.planning/phases/06.3-data-layer-refinement/06.3-01-SUMMARY.md`
</output>
